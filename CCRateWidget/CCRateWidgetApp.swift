import SwiftUI

@main
struct CCRateWidgetApp: App {
    var body: some Scene {
        Window("Claude Rate Widget", id: "main") {
            ContentView()
        }
        .defaultSize(width: 480, height: 400)
    }
}
